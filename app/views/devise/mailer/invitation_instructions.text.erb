<%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %>

<p><%= @resource.invited_by.username %> has invited you to become <%= @resource.invitation_message %></p>


<%= @resource.invitation_message %>

<%= accept_invitation_url(@resource, invitation_token: @token) %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
<% end %>

<%= t("devise.mailer.invitation_instructions.ignore") %>

<p> If you dont want to receive any invitation in future then you can unsubscribe using the link below </p>
<%= link_to "Unsubscribe", mailkick_unsubscribe_url %>
